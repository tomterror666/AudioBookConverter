<?xml version="1.0" encoding="utf-8"?>
<Project>
  <!--
    WinUI2xVersion must live at repo root (not only windows/ExperimentalFeatures.props): Microsoft.ReactNative under
    node_modules does not import ExperimentalFeatures; without this pin you get WinUI 2.8 + Microsoft.WindowsAppSDK
    1.7 markup interop → WMC9999 (XamlType/DirectUIXamlType) on VS 2026.

    This file must be committed to Git — a fresh clone without it will hit WMC9999 again.

    RestoreLockedMode=false: RNW ships packages.lock.json; with WinUI2xVersion 2.7.3 the lock can disagree (NU1004).
    Do not set RestorePackagesWithLockFile=false — NuGet NU1005 if a packages.lock.json exists.
  -->
  <PropertyGroup>
    <WinUI2xVersion>2.7.3</WinUI2xVersion>
    <RestoreLockedMode>false</RestoreLockedMode>
  </PropertyGroup>
</Project>
