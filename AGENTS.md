# Hinweise für KI-Assistenten

Persistent kontext für dieses Projekt: **`.cursor/rules/*.mdc`** und **`docs/cursor-and-handoff.md`** (Plattformen, Pfade, Pipeline, was nicht im Repo liegt).

AudioBookConverter: React Native, **Konvertierungs-Bridge primär macOS** (`DependencyStatus.mm` + `scripts/*.py`).
