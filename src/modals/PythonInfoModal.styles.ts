import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  cardWide: {
    maxWidth: 680,
    width: "100%",
  },
  scroll: {
    maxHeight: 520,
  },
  scrollContent: {
    paddingBottom: 8,
  },
});
